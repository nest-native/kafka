{
  "name": "nest-native-kafka-sample-07-request-reply",
  "version": "0.0.0",
  "private": true,
  "description": "Sample 07 - opt-in request-reply: @KafkaHandler({ reply: true }) answering KafkaRequestReplyService.request(), tested with KafkaTestModule",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "ts-node --transpile-only scripts/smoke.ts",
    "test": "npm run typecheck && npm run smoke",
    "start": "ts-node --transpile-only src/main.ts"
  },
  "dependencies": {
    "@nestjs/common": "11.2.1",
    "@nestjs/core": "11.2.1",
    "@nest-native/kafka": "0.5.1",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "ts-node": "10.9.2",
    "typescript": "6.0.3"
  }
}
